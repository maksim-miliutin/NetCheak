{
    "name": "netcheck",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev:api": "npm run dev --workspace @netcheck/api",
        "dev:web": "npm run dev --workspace @netcheck/web",
        "test": "vitest run",
        "typecheck": "tsc --noEmit -p apps/api",
        "build:binary": "node tools/build-binary.mjs"
    },
    "devDependencies": {
        "esbuild": "^0.28.2",
        "vitest": "^3.2.7"
    }
}
